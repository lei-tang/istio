apiVersion: v1
kind: Service
metadata:
  name: protomutate
  namespace: istio-system
  labels:
    istio: protomutate
spec:
  ports:
    - port: 443
      name: https-validation
  selector:
    istio: protomutate
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: istio-protomutate-service-account
  namespace: istio-system
  labels:
    app: security
    chart: security
    heritage: Tiller
    release: istio
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: istio-protomutate
  namespace: istio-system
  labels:
    app: security
    chart: security
    heritage: Tiller
    release: istio
    istio: protomutate
spec:
  replicas: 1
  selector:
    matchLabels:
      istio: protomutate
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: security
        chart: security
        heritage: Tiller
        release: istio
        istio: protomutate
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      serviceAccountName: istio-protomutate-service-account
      containers:
        - name: protomutate
          image: "gcr.io/endpoints-authz-test1/protomutate:chiron-develop-7-29-2019"
          imagePullPolicy: Always
          resources:
            requests:
              cpu: 10m
          volumeMounts:
          - mountPath: /etc/webhookcerts
            name: webhookcerts
            readOnly: true
      volumes:
        - name: webhookcerts
          secret:
            defaultMode: 420
            secretName: istio.webhook.protomutate